#include<stdio.h> 
#include<conio.h> 
 
int main() { 
    int n, key, i; 
    int roll_no[15]; 
    int foubd = 0; 
 
    clrscr(); 
 
    printf("Enter the number of students: "); 
    scanf("%d", &n); 
 
    for(i = 0; i < n; i++){ 
    printf("Enter roll number of students %d; ",i+1); 
    scanf("%d",&roll_no[i]); 
    } 
    printf("Enter the roll no to search: "); 
    scanf("%d",&key); 
 
    for(i=0;i< n; i++) { 
    if(roll_no[i] == key) { 
        printf("Attended"); 
        found =i; 
        Break; 
    } 
 } 
 if(!found){ 
prtintf("not attended")_; 
} 
getch(); 
return 0; 
}
